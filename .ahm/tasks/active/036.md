---
id: 036
title: Add documentation freshness checks to CI
status: Pending
priority: P3
effort: S
labels: type:ci, area:dev-env
exec_plan: -
depends_on: -
created: 2026-07-03T10:00:00-04:00
updated: 2026-09-22T17:10:00-04:00
---
# Add documentation freshness checks to CI

**Based on `docs` audit finding** — `freshness_enforcement` scored 2/3 because documentation freshness rests entirely on manual diligence with no mechanical enforcement.

## Problem

All internal markdown links across the doc set (`AGENTS.md`, `ARCHITECTURE.md`, `README.md`, `CONTRIBUTING.md`, `docs/guardrails/`, `docs/adr/`) resolve today, but nothing prevents them from breaking as files are renamed, moved, or deleted. A stale or broken link silently degrades agent and contributor navigation.

There is no CI job that checks:
- Internal markdown link resolution (links to other `.md` files in the repo).
- Anchor/heading link resolution within files.
- Whether files referenced in `ARCHITECTURE.md`'s codemap still exist.

A second class of drift has the same shape: the CLI surface in `README.md` and
`HelpText` is checked by nothing. That scope was folded in on 2026-09-22; see
[Scope Addition](#scope-addition-2026-09-22-cli-documentation-parity).

## Relevant Files

### Existing (review before changing)
- `.github/workflows/ci.yml` — the CI workflow that runs `just ci`.
- `justfile` — the command catalog; `just ci` is the full gate.
- `.pre-commit-config.yaml` — local pre-commit hooks (code-only today).

### To create or modify
- `.github/workflows/ci.yml` — add a doc-link-check step to the existing CI job.
- `justfile` — add a `just docs-check` recipe for local use.
- `docs/guardrails/documentation.md` — document the check as a required surface.

## Grooming Decision (2026-07-03)

**Chosen tool: `lychee` via `lycheeverse/lychee-action`.**

Rationale:
- Mature, widely used, actively maintained single-binary link checker.
- The `lycheeverse/lychee-action` GitHub Action is a zero-config install — a single step in CI with no Node.js or Go toolchain overhead for this purpose.
- Handles anchor links, internal `.md` links, and code-block detection out of the box.
- Supports `.lychee.toml` config for excluding directories and URL patterns.
- Fallback if action proves problematic: `markdown-link-validator` (Go-native, same toolchain as the project). Not chosen as primary because `lychee` is more battle-tested for link-checking specifically.

Use `lycheeverse/lychee-action` v2.5.0 pinned to commit
`5c4ee84814c983aa7164eaee476f014e53ff3963`; do not use a floating tag. The
tested local CLI version is lychee 0.24.2. The local recipe may require that
binary to be installed, but CONTRIBUTING must name the tested version so local
and CI behavior are reproducible.

## Fix Direction

Add a markdown link checker step to the existing CI workflow using `lychee`. The checker must:

1. Resolve all internal markdown links (e.g., `[text](./relative/path.md)`, `[text](../path.md)`, `[text](path.md#anchor)`).
2. Exit non-zero when any link is broken (not `continue-on-error: true`).
3. Be fast enough to not meaningfully increase CI runtime.

### Implementation

Add a step to `.github/workflows/ci.yml`:

```yaml
- name: Check markdown links
  uses: lycheeverse/lychee-action@5c4ee84814c983aa7164eaee476f014e53ff3963 # v2.5.0
  with:
    args: --config .lychee.toml --no-progress -- './**/*.md'
  env:
    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

Create `.lychee.toml`:
```toml
# Only check internal .md links — external URL checking is out of scope.
# Exclude generated/task directories.
exclude = [
  '^https?://',
  '^\./\.git/',
  '^\./\.ahm/',
  '^\./dist/',
]
```

Also add `just docs-check` to `justfile`:
```makefile
docs-check:
    lychee --config .lychee.toml --no-progress './**/*.md'
```

And update `docs/guardrails/documentation.md` to mention the link-check step as a required CI verification surface.

### Acceptance criteria

- [ ] CI job or step checks all `*.md` files (except generated/task files) for broken internal links.
- [ ] A broken internal link in any checked file causes CI to fail.
- [ ] `justfile` gets a `just docs-check` recipe (or similar) so the check is runnable locally.
- [ ] `docs/guardrails/documentation.md` is updated to mention the link-check step as a required verification surface.
- [ ] All existing internal links pass the check before the CI job is enabled.
- [ ] No new runtime dependency is introduced to the project itself (CI-only tool).
- [ ] CLI documentation parity is checked in the same gate: a flag registered in
      `cmd/cake-repl/main.go` but absent from `README.md`, or a slash command or
      key binding in `HelpText` absent from `README.md`, fails the suite.

## Scope Addition (2026-09-22): CLI documentation parity

Folded in from the agent-instruction review of the preflight skill and its
routing.

### Problem

`README.md` documents the CLI in a flag table and the slash commands and key
bindings in prose. `cmd/cake-repl/main.go` registers the flags. `HelpText` in
`internal/app/commands.go` mirrors the commands and key bindings. Nothing
asserts the three agree — `internal/app/update_test.go` only compares
`HelpText` with itself.

The [documentation guardrail](../../../docs/guardrails/documentation.md) impact
matrix makes `README.md` and `HelpText` required for CLI, command, and
key-binding changes, so this is a rule enforced by diligence alone: the same
weakness 036 exists to fix for links.

Evidence at the time of writing: `README.md`'s flag table and
`cmd/cake-repl/main.go`'s registrations agree (23 flags each), and the six
slash commands and ten key bindings in `HelpText` match `README.md`. There is
no drift today; the point is that nothing would fail if one side changed alone.

### Direction

`lychee` cannot do this — it resolves paths and anchors, not Go source. The
check must read the Go side, so it belongs in the test suite:

- A test in `cmd/cake-repl` that walks `flag.CommandLine.VisitAll` (or a small
exported flag list) and asserts every registered flag name appears in
`README.md`.
- A test in `internal/app` asserting the command names and key bindings in
`HelpText` appear in `README.md`.
- Keep one check per surface, against the existing table and constant, with no
new generated file. If a generated doc becomes unavoidable, the documentation
guardrail's rule against hand-editing generated artifacts applies.
- Failing CI is the point; a warning-only check is not accepted.

### Acceptance criteria (parity)

- [ ] Adding a flag in `main.go` without documenting it in `README.md` fails
      `just test`.
- [ ] Adding a slash command or key binding without documenting it in
      `README.md` fails `just test`.
- [ ] The failure message names the missing flag, command, or binding.
- [ ] No new tool or runtime dependency; the check rides in `just test`, so
      `just ci` covers it with no new CI step.

## Constraints

- The check must run on every PR and push to master, not on a schedule.
- Must not check external/HTTP URLs (those are out of scope for doc freshness; link rot from external URLs is a separate concern).
- Must not check `./.ahm/` or `./dist/` — these are generated or agent working records.
- Must not break on code blocks containing markdown-style links (some tools do this unless configured).
- Prefer a tool that the project already has a runtime for (Go) to minimize CI setup complexity.
