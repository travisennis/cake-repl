---
id: 023
title: Add /model and /profile slash commands
status: Open
priority: P3
effort: M
labels: type:feature, area:cli, area:model
exec_plan: -
depends_on: 014
created: 2026-06-18T16:37:52-04:00
updated: 2026-06-18T16:37:54-04:00
---
# Add /model and /profile slash commands

**Based on audit direction finding DIR-01** — audit commit `fe9b393`

## Problem

`--model` and `--profile` are startup-only CLI flags (`cmd/cake-repl/main.go:28-29`).
There is no way to switch model or profile mid-session without quitting and
restarting. Users experimenting with different models — a common workflow
with coding assistants — must exit and re-enter the REPL each time.

The session state machine already demonstrates how mid-session state changes
work: `/new`, `/continue`, `/resume` all modify what the next `cake.Start`
call uses. `/model` and `/profile` would follow the same pattern.

## Current status

Deferred to backlog (grooming 2026-06-27). Dependency 014 is completed; next step when picked up is creating an ADR.

## Investigation needed

1. **State location** — `Model` and `Profile` currently live on `Config`
   (immutable at startup). They need to move to mutable state on the Model
   or `sessionState` so `startRun` can read the current value. `Config`
   keeps the initial values as defaults.

2. **Command shape** —
   - `/model` with no arg: show current model.
   - `/model <name>`: set the model for the next prompt.
   - `/model default` or `/model ""`: clear the model override (use cake's
     default).
   - Same for `/profile`.

3. **Status line** — `view.go:statusLine()` already shows `model <name>`
   when set. It should reflect the current value after a `/model` command.

4. **HelpText and README** — add `/model` and `/profile` to the slash
   commands table in `HelpText` (`commands.go`), `README.md`, and
   `AGENTS.md` routing.

5. **ADR required** — this is a `type:feature` task that changes user-visible
   behavior and the command surface. Create an ADR before implementation
   per `docs/adr/README.md`.

## Relevant files

- `cmd/cake-repl/main.go:28-29` — `--model`, `--profile` flags
- `internal/app/model.go:22-30` — `Config` struct (Model, Profile fields)
- `internal/app/model.go:49-70` — `New()` (initial model construction)
- `internal/app/update.go:185-196` — `startRun` (reads `m.cfg.Model`, `m.cfg.Profile`)
- `internal/app/commands.go:8-18` — `CommandKind` constants
- `internal/app/commands.go:45-72` — `ParseCommand` switch
- `internal/app/completion.go:6-16` — `knownCommands` (or `commandTable`
  if task 014 is done first)
- `internal/app/view.go:38-48` — `statusLine` (shows current model)
- `internal/app/commands.go:75-95` — `HelpText`
- `README.md` — slash commands table
- `AGENTS.md` — CLI and slash commands routing

## Constraints

- Must not break `--model`/`--profile` CLI flags (they set the initial value).
- Must work with `--no-color`.
- `just ci` must pass.
- ADR must be created before code changes.

## Dependencies

- Depends on task 014 (unify command list) — adding new commands to a
  single `commandTable` is cleaner than adding to two separate lists.

## Acceptance notes (draft — refine after ADR)

- [ ] ADR created for `/model` and `/profile` slash commands
- [ ] `/model <name>` sets the model for the next prompt
- [ ] `/model` with no arg shows the current model
- [ ] `/profile <name>` sets the profile for the next prompt
- [ ] `/profile` with no arg shows the current profile
- [ ] Status line reflects the current model/profile after a change
- [ ] `HelpText`, `README.md`, and `AGENTS.md` updated
- [ ] `just ci` passes

## Maintenance note

If cake adds more pass-through flags (e.g., `--temperature`), the same
pattern extends: add a slash command that mutates the model state read by
`startRun`.

