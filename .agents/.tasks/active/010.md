---
id: 010
title: Tool output expansion — view full truncated output on demand
status: Open
priority: P4
effort: M
labels: type:feature, area:ui
exec_plan: -
depends_on: -
created: 2026-06-16T15:10:07-04:00
---
# Tool output expansion — view full truncated output on demand

**Based on audit direction finding DIR-02** — audit commit `05f0916`

## Problem

Tool output is truncated at `DefaultOutputLimit` (2000 characters) with a marker `"\n… truncated (%d bytes total)"`. The full output is available in `FunctionCallOutput.Output` and stored in the item data, but there's no way to expand it in place.

The truncation is in `internal/ui/toolblock.go:103`:
```go
out := TruncateOutput(tool.Output, DefaultOutputLimit)
lines = append(lines, indent(th.ToolOutput.Width(width-2).Render(out), "  "))
```

## Investigation needed

1. **Selection model** — how does the user pick which tool block to expand? Options:
   - Expand the last tool block automatically when truncated.
   - Add a key binding like `Ctrl+E` that toggles the most recent tool block.
   - Click-to-expand (not available in Bubble Tea textarea/viewport — would need mouse event handling).
   - Inline hint like `[expand]` at the truncation line that is a clickable/shortcut.
2. **Rendering impact** — expanding replaces the cached rendered item with a new full rendering. Must keep the viewport position stable.
3. **Collapse back** — the user should be able to toggle between truncated and full view.
4. **Output limit interaction** — if `--output-limit` (task 005) is implemented, expansion should show the full output regardless of the limit.

## Relevant files

- `internal/ui/toolblock.go` — `TruncateOutput`, `renderTool` call sites
- `internal/ui/timeline.go` — `ToolBlock` struct, `Item` rendering
- `internal/app/update.go` — `applyEvent`, `FunctionCallOutput` handling
- `internal/app/keys.go` — new key binding
- `internal/app/model.go` — expanded state tracking

## Constraints

- Must work with `--no-color`.
- Must not re-render the full timeline on toggle.
- The truncated/full state should be per-item, not global.

## Acceptance notes (draft — refine during grooming)

- [ ] Key binding toggles truncated/full view for the target tool block
- [ ] Viewport position is preserved after toggle
- [ ] `just ci` passes
- [ ] README updated

