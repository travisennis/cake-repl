---
id: 019
title: Add completion.go to ARCHITECTURE.md module map
status: Completed
priority: P3
effort: XS
labels: type:docs, area:dev-env
exec_plan: -
depends_on: -
created: 2026-06-18T16:37:44-04:00
updated: 2026-06-22T07:55:05-04:00
---
# Add completion.go to ARCHITECTURE.md module map

**Based on audit finding DOCS-02** — audit commit `fe9b393`

## Problem

`ARCHITECTURE.md` has a module map for `internal/app/` that lists
`model.go`, `update.go`, `session.go`, `commands.go`, `keys.go`,
`history.go`, `view.go` but is missing `completion.go`, which was added in
commit `fe9b393` (tab completion feature).

Current module map (ARCHITECTURE.md:24-31):

```
internal/app/            Bubble Tea state machine (the REPL itself).
  model.go               Model struct, layout, timeline render cache.
  update.go              Event/key/command handling (the Update loop).
  session.go             sessionState: pure run-mode state machine.
  commands.go            Slash-command parsing + HelpText.
  keys.go                Key bindings.
  history.go             In-memory prompt history.
  view.go                Top-level View composition.
```

## Relevant files

- `ARCHITECTURE.md:24-31` — module map for `internal/app/`

Out of scope: any other documentation changes.

## Fix direction

Add one line to the module map after `commands.go`:

```
  completion.go          Tab completion for slash commands.
```

## Acceptance notes

- [x] `ARCHITECTURE.md` module map includes `completion.go` (line 42)
- [x] No code changes

## Maintenance note

When adding a new file to `internal/app/`, add it to the module map.

