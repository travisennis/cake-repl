---
id: 016
title: Add tests for handleTabComplete cycling state machine
status: Completed
priority: P2
effort: S
labels: type:test, area:dev-env
exec_plan: -
depends_on: -
created: 2026-06-18T16:37:35-04:00
updated: 2026-06-20T21:41:56-04:00
---
# Add tests for handleTabComplete cycling state machine

**Based on audit finding TESTS-02** — audit commit `fe9b393`

## Problem

`internal/app/update.go:73-117` — `handleTabComplete` implements Tab
completion cycling with wrap-around, input-change detection, and a
single-match shortcut. `completion_test.go` tests `completeSlash` (the
matching function) but not the cycling state machine in `handleTabComplete`.

The cycling state (`completionPrefix`, `completionMatches`, `completionIdx`)
on the Model is untested. The wrap-around logic (restore original prefix on
full cycle) and the input-change reset are edge-case-prone.

## Relevant files

- `internal/app/update.go:73-117` — `handleTabComplete`
- `internal/app/completion.go` — `completeSlash` (already tested)
- `internal/app/completion_test.go` — existing tests for `completeSlash`
- `internal/app/model.go:40-44` — completion state fields on Model
- `internal/app/model_test.go` — `newLaidOutModel` helper

Out of scope: testing `completeSlash` itself (already covered in
`completion_test.go`).

## Fix direction

Add tests to a new `internal/app/tabcomplete_test.go`:

1. **Single match completes directly** — input `/h`, Tab → input becomes
   `/help`, no cycle state set.
2. **Multiple matches start cycle** — input `/q`, Tab → input becomes
   `/quit`, `completionPrefix` is `/q`, `completionMatches` is `["/quit", "/q"]`.
3. **Cycle advances** — from the above state, Tab again → input becomes `/q`.
4. **Cycle wraps around** — from the last match, Tab again → input restores
   to original prefix `/q`, cycle state reset.
5. **Input change resets cycle** — start a cycle, then change the input,
   Tab → fresh match cycle starts.
6. **Non-slash input is a no-op** — input `hello`, Tab → no change.

Call `handleTabComplete` directly on a `Model` (via `m.handleTabComplete()`)
and verify `m.input.Value()` and completion state fields after each call.

## Bug discovered during implementation

Scenarios **3** (cycle advances) and **4** (cycle wraps around) **do not work**
with the current production code.  The root cause is in `update.go:127`:

```go
if m.completionPrefix != "" && input != m.completionPrefix {
```

After `handleTabComplete` completes a slash match (e.g., `/q` → `/quit`),
`completionPrefix` is set to the original input (`/q`) but the input field now
holds the first match (`/quit`).  On the next Tab press, the check sees
`input != m.completionPrefix` (`/quit` != `/q`), treats the completion result
as a user edit, and resets the cycle instead of advancing it.

The fix is to remove or replace the `input != m.completionPrefix` comparison
— the `m.completionPrefix == ""` check at line 142 already handles fresh
starts correctly.  However, since this task is scoped to test-only changes,
the tests document the actual (broken) behavior in
`TestHandleTabCompleteSecondTabReset`.

## Acceptance notes

- [x] `go test github.com/travisennis/cake-repl/internal/app` passes
- [x] `just test` passes
- [x] At least 5 test cases covering the scenarios above
      — 6 subtests in `TestHandleTabComplete`, plus
        `TestHandleTabCompleteSecondTabReset` documenting the bug.
- [x] No changes to production code
      — only `internal/app/tabcomplete_test.go` added.

**Bug note:** Scenarios 3 (cycle advance) and 4 (cycle wrap) are broken in
production code (`update.go:127`).  Tests document the actual behavior;
see the Bug discovered section above.

## Maintenance note

If the completion cycling behavior changes (e.g., different wrap-around
semantics), update these tests to match.

