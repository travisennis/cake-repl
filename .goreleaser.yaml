version: 2

project_name: cake-repl

before:
  hooks:
    - go mod tidy -diff

builds:
  - id: cake-repl
    main: ./cmd/cake-repl
    binary: cake-repl
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w -X github.com/travisennis/cake-repl/internal/version.Binary={{ .Version }}

archives:
  - id: cake-repl
    ids:
      - cake-repl
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    files:
      - README.md

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  use: git
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  github:
    owner: travisennis
    name: cake-repl
